export const regexCreditScore = /^[0-5](\.[0-9])?$/; // Positive number (0-5) with optional one decimal place
export const regexPhoneNumber = /^[0-9]{8}$/; // Exactly 8-digit phone number
export const regexId = /^\d+$/; // Positive integer
